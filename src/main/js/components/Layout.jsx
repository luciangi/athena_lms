import React from "react";
import Menu from "./Menu";

const Layout = () => {
    return (
        <div>
            <Menu/>
            <span>Layout</span>
        </div>
    );
};

Layout.propTypes = {};

export default Layout