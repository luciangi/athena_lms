export default (state = {}, action) => {
    // switch (action.type) {
    //     case menuConstants.LOGIN_OPEN: {
    //         return { ...state, loginOpened: true, error: false }
    //     }
    //     case menuConstants.LOGIN_CLOSE: {
    //         return { ...state, loginOpened: false, error: true }
    //     }
    //     case menuConstants.LOGIN_ERROR: {
    //         return { ...state, error: true }
    //     }
    // }
    return state
}
